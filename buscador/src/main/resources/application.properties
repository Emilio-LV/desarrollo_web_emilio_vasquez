spring.application.name=buscador

# Misma base que la app Flask (esquema tarea2, usuario cc5002).
spring.datasource.url=jdbc:mysql://localhost:3306/tarea2?useUnicode=true&characterEncoding=UTF-8
spring.datasource.username=cc5002
spring.datasource.password=programacionweb
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# No modificamos las tablas existentes.
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# Crea la tabla nota desde schema.sql (tabla-nota.sql del enunciado).
spring.sql.init.mode=always
